When performing a code review, check to see if we should be writing test cases for fee bump transactions. Do this by checking if the PR is adding tests for regular transactions, and if so, comment and ask if we should also add a test for fee bump transactions. Also check if the PR is modifying any code that could relate to fee bump transactions, and if so, comment and ask if tests for fee bump transactions should be added or updated accordingly.
